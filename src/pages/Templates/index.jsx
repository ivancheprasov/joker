import MainLayout from "../../components/MainLayout";
import {useDispatch} from "react-redux";

const Templates = ({selectedTab}) => {
    const dispatch = useDispatch();
    return (
        <MainLayout>
        </MainLayout>
    );
};

export default Templates;